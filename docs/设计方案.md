# 质量趋势图 · 设计方案

| 项目 | 内容 |
|---|---|
| 定位 | 生产看板二期扩展，对应主方案「可选扩展」 |
| 前置依赖 | 待确认清单业务第 6 题——车间是否需要显示良品率 / 不良品数 |
| 展示环境 | 二楼车间 MAXHUB 大屏（与主看板同一块屏，轮播或分页切换） |
| 原型 | `quality.html`，双击即可打开，用内置模拟数据 |
| 更新日期 | 2026-07-29 |

主看板项目在另一个仓库：[production-dashboard](https://github.com/discipline-dcy/production-dashboard)，
本文提到的「主方案」「待确认清单」均指该仓库 `docs/` 下的文档。

---

## 一、先说清楚这张图给谁看

不是给品质部做分析用的。品质工程师要的是 SPC 控制图、Cpk、分层抽样，那是另一套工具，
坐在电脑前看。

这块屏幕的读者是**班组长和车间主任，站在五米外，抬头看三秒**。三秒内他要拿到的
只有三件事：

1. 现在的良品率是多少，**够不够目标**
2. 今天什么时候开始变坏的
3. **是哪条线**

拿不到第 3 条，前两条就没有用——看见数字掉了却不知道去找谁，看板就退化成了装饰。
下面所有取舍都是围着这三条做的。

---

## 二、版面

```
┌──────────────┬───────────────────────────────────────────────┐
│ 本班次良品率  │ 良品率趋势        本班次 · 每小时              │
│    97.9%     │ 纵轴 95–100%（非零起点）                       │
│  ◆ 预警      │      目标 98.5% ┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈           │
│  ↓0.5 个百分点│    ●──●──●╮                       ╭●  98.4%   │
│              │            ╰──●╌╌○╌╌●                        │
├──────────────┤                    96.3%                      │
│ 不良品累计    ├───────────────────────────────────────────────┤
│    86 件     │ 不良品数          件 / 小时 · 纵轴自 0 起      │
│  ↑12 件      │                        27                     │
│              │  ▁  ▂  ▂  ▅  ▁  █  ▁                          │
├──────────────┴───────────────────────────────────────────────┤
│ 各产线良品率    近 7 小时 · 六张小图共用同一纵轴               │
│ 一号线 99.1% ▂▂▂  二号线 98.6% ▂▂▂  三号线 96.4% ▃▂▁         │
│     ● 达标           ● 达标            ■ 超标                 │
│ 四号线 98.6% ▂▂▂  五号线 99.4% ▃▃▃  六号线 97.0% ▃▂▂         │
│     ● 达标           ● 达标            ◆ 预警                 │
└──────────────────────────────────────────────────────────────┘
```

三块，对应上面三个问题：**左侧回答「够不够」，中间回答「什么时候」，下方回答「是谁」。**

---

## 三、六个关键决策

每条都附上「不这么做会怎样」——因为这些取舍看起来都可以随便改，改了就出事。

### 1. 纵轴不从 0 起（折线图）

良品率的实际波动区间是 96%–99.5%。从 0 画到 100，这条线是一条**直线**，
一整块屏幕什么都没说。

所以折线图纵轴取 `floor(min(数据最小值, 目标值) - 0.5)` 到 100，本例是 95–100%。

截断纵轴是有代价的——它会放大波动。补偿措施有三条，缺一不可：

- 图上**显式写出「纵轴 95–100%（非零起点）」**
- **目标线是真正的锚点**：读者判断高低靠的是「在虚线上还是下」，不是「离底边多远」
- **不画面积填充**。面积的高度隐含「从 0 累计到这里」的意思，纵轴一截断这个意思就是假的
  （原型第一版画了，看了一眼就删掉——那块蓝色在告诉人一个不存在的量）

> **柱状图纵轴必须从 0 起**，没有例外。柱子的长度**就是**数值，截断等于直接说谎。
> 所以下面那张不良品数柱状图是 0 起点，和上面那张不一样，这不是不统一，是两种图的规矩不同。

### 2. 良品率和不良品数拆成上下两张图，不做双纵轴

最容易想到的做法是一张图：左轴良品率（%），右轴不良品数（件），线和柱叠在一起。

**不要做。** 两个纵轴怎么对齐完全是主观的——把右轴的量程调一调，就能让两条线看起来
"高度相关"或者"毫无关系"。这张图会**凭空造出数据里没有的结论**，是图表里最常见也最
严重的错误。

正确做法：两张独立的图，上下堆叠，**左右内边距设成完全一样**，于是时间轴天然对齐。
读者照样能上下对照着看（13 点良品率的谷底，正对着不良品数的峰值），但每张图只有一个
纵轴，没有任何东西被"凑"到一起。

### 3. 六条产线不画成六条折线，而是六张小图

六条折线叠在一张图上，在五米外就是一团蓝黑色的毛线。而且需要六种颜色，
超出了色盲安全的上限。

改成**小倍数**：一条线一张迷你折线，各自直接标出当前值和状态。

**关键约束：六张小图必须共用同一个纵轴范围**（本例 95–100%）。
如果每张图各自适配自己的数据范围，五号线那条 99.2%–99.5% 的微小波动会被拉成一条
剧烈起伏的曲线，看起来比真正出问题的三号线还惊心动魄。**那是在骗人。**

### 4. 只有一个序列色——这条是被工具否掉后改的

原设计：良品率折线用蓝 `#3987e5`，不良品数柱状用橙 `#d95926`（配色表的第 2 号槽位）。
跑配色校验脚本，直接不通过：

```
[FAIL] Normal-vision  #d95926 ↔ #d03b3b（状态色·超标）  ΔE 6.8  —— 低于 15，正常视力都难分辨
[FAIL] CVD separation #d95926 ↔ #0ca30c（状态色·达标）  ΔE 1.4（绿色盲）—— 基本同色
```

也就是说，橙色柱子和红色"超标"标记、绿色"达标"标记在同一块屏幕上会互相冒充。
车间大屏上"红色"是有含义的，绝不能让一个纯粹表示数量的柱子看起来像在报警。

**改法：整个页面只用一个序列色（蓝）。** 良品率和不良品数分属两张独立的图，各自
只有一个序列，本来就不需要靠颜色区分身份——**是标题在区分它们，不是颜色**。
第二个颜色是凭空发明的，发明出来就闯祸。

改完复验，蓝色与三个状态色两两都在安全距离之外（最差 ΔE 25.7）：

| 配对 | 色盲视角 ΔE | 正常视力 ΔE | 判定 |
|---|---|---|---|
| 蓝 ↔ 达标绿 | 27.9 | 30.7 | 通过 |
| 蓝 ↔ 预警黄 | 32.4 | 37.6 | 通过 |
| 蓝 ↔ 超标红 | 25.7 | 31.9 | 通过 |

> 状态色三档之间（红 ↔ 绿 ΔE 4.1）依旧是分不开的——这是已知的，和主看板同一个问题，
> 靠**形状 + 文字**解决：● 达标 / ◆ 预警 / ■ 超标。颜色永远只是辅助通道。

### 5. 样本量太小的时间点要标出来

这条最容易被漏掉，也最容易让看板闹笑话。

12 点午休，那一小时只做了 115 件，坏了 4 件 → 良品率 96.5%。画在图上就是一个
**吓人的深坑**。但它什么也不说明：小批量下坏一件就是 0.9 个百分点。班组长照着这个
坑去查产线，查不出任何东西，查两次以后他就再也不看这块屏幕了。

处理方式：

| 项目 | 做法 |
|---|---|
| 判定 | 该小时产量 < `minSample`（默认 200 件，需按实际节拍调整） |
| 数据点 | 画成**空心圈**，不是实心点 |
| 线段 | 与之相连的两段线降到 35% 透明度 |
| 柱子 | 降到 45% 透明度 |
| 时间标签 | 一并淡化 |
| 图上说明 | 「空心点 ＝ 该小时产量不足 200 件，比率仅供参考」 |

### 6. 大屏没有鼠标，所有数值必须静态可读

这是这块屏幕和普通网页仪表盘最根本的区别。一般图表把数值藏在 tooltip 里，
鼠标悬上去才显示。**挂在车间墙上的屏幕没有鼠标**，藏进去的值等于不存在。

因此：

- **直接标注**只标两个点：**末点**（现在多少）和**极值**（最差的那个小时）。
  每个点都标数字，五米外就是一片糊掉的白字，等于没标
- 其余数值由**纵轴刻度**承担
- 每张图都配一份**表格视图**（页面底部，默认折叠），任何一个数都查得到
- tooltip 只在「大屏支持触摸」或「有人用电脑打开同一个页面」时才有意义——
  取决于待确认清单业务第 8 题的答复，**不是这块屏幕的主要读取方式**

---

## 四、接口设计

沿用主方案的思路：前端只认接口，后端数据源怎么换都不影响。

```
GET /api/quality
```

```json
{
  "updatedAt": "2026-07-27T06:27:39.000Z",
  "shift": "白班",
  "target": 98.5,
  "minSample": 200,
  "summary": {
    "rate": 97.9,
    "defect": 86,
    "output": 4185,
    "rateDelta": -0.5,
    "defectDelta": 12,
    "deltaBase": "上一班次"
  },
  "series": [
    { "t": "08:00", "output": 620, "defect": 8, "rate": 98.7 }
  ],
  "lines": [
    { "id": 1, "name": "一号线 · 装配", "spark": [99.2, 99.0, 98.8] }
  ]
}
```

字段说明：

| 字段 | 含义 | 备注 |
|---|---|---|
| `target` | 良品率目标线 | 来源待确认，见下一节 |
| `minSample` | 样本不足阈值（件 / 小时） | 按实际节拍设定，宁可设高不设低 |
| `summary.rateDelta` | 相对 `deltaBase` 的**百分点**差 | 不是百分比，别混 |
| `series[].rate` | 后端算好直接给 | 前端不做除法，避免分母口径两边不一致 |
| `lines[].spark` | 与 `series` 同长度、同时间刻度 | 长度不一致前端不做对齐，直接按位画 |

状态三档由前端按 `target` 判定，不走接口——阈值逻辑只存在于一处：

| 状态 | 条件 | 标记 |
|---|---|---|
| 达标 | `rate >= target` | ● |
| 预警 | `target - 1.5 <= rate < target` | ◆ |
| 超标 | `rate < target - 1.5` | ■ |

---

## 五、必须先问清楚的口径问题

**这些不问清楚，图画得再对也是错的。** 建议追加到待确认清单业务侧：

| # | 问题 | 为什么要问 |
|---|---|---|
| 1 | 良品率的**分母**是投入数还是产出数？ | 两种算法结果能差一两个百分点，跟品质部报表对不上就没人信这块屏 |
| 2 | 要看**一次合格率（FPY）**还是**最终合格率**？ | 返修后合格的算不算良品，是完全不同的两个指标，改善方向也不同 |
| 3 | **返工品**怎么计？ | 同上，且直接影响不良品数的口径 |
| 4 | 目标值 98.5% 从哪来？各产线是否**不同目标**？ | 若各线目标不同，小倍数里的目标虚线要各画各的，接口得按产线下发 target |
| 5 | 不良数据**多久录入一次**？ | 若质检是两小时汇总一次录入，"每小时"这个粒度就是假的，得改成按班次或按批次 |
| 6 | 是否需要**不良原因**分类？ | 见下节 |

> 第 5 条尤其关键。它决定了这张图的**时间粒度到底能做多细**。
> 如果检验数据实际是隔两小时才进系统，画成每小时的折线就是在用插值糊弄人。

---

## 六、有意没做的东西

| 没做 | 原因 |
|---|---|
| **不良原因 Pareto 图** | 这是「知道了是三号线之后，接着查为什么」的下一步，价值很高，但取决于口径问题第 6 条——数据源里到底有没有原因字段。有的话建议做，一张横向条形图，Top 5 + 其他，单色 |
| SPC 控制图 / Cpk | 品质部的分析工具，读者、场景、精度要求都不一样，不该挤进车间大屏 |
| 良品率排行榜 | 排名会诱发产线之间对着数字博弈（挑简单的单子做），对质量本身没帮助 |
| 缺陷率热力图（产线 × 小时） | 六条线 × 十二小时的格子，五米外看不清；小倍数已经覆盖了同样的信息 |

---

## 七、落地方式与工作量

原型 `quality.html` 目前用内置模拟数据，双击就能看。接入真实数据只需两步
（`server.js` 在主看板仓库 [production-dashboard](https://github.com/discipline-dcy/production-dashboard)）：

1. `server.js` 增加 `/api/quality` 路由，参照现有 `readProduction()` 写一个 `readQuality()`
2. `quality.html` 里的 `loadQuality()` 改成 `fetch('/api/quality')`——文件里已注释好

渲染逻辑（`renderTrend` / `renderDefects` / `renderSparks` / `renderTable`）**一行都不用动**。

图表全部是**手写 SVG，零第三方依赖**，与主方案「不引框架、单体部署」的选型一致。
没有引入 ECharts 之类的图表库，理由和不引 React 一样：一个 SVG 折线图的代码量比
接一个图表库的配置项还少，而且不用担心 MAXHUB 上的老浏览器内核跑不动。

工作量估计（**前提是口径问题已答复、数据源已确定**）：

| 项目 | 工作量 |
|---|---|
| 后端 `readQuality()` + 路由 | 0.5–1 天（若与生产进度同一数据源） |
| 前端接入 + 现场字号调整 | 0.5 天 |
| 与主看板的轮播/分页切换 | 0.5 天 |

口径问题若未答复，**不要开工**——分母定义改一次，后端的取数逻辑就要重写一次。

---

## 附：自检清单

发布前逐条核对，这些都是图表最容易出的错：

- [x] 没有双纵轴
- [x] 柱状图纵轴从 0 起；折线图截断纵轴已显式标注
- [x] 截断纵轴上没有面积填充
- [x] 配色经脚本校验，未靠肉眼判断
- [x] 状态不靠颜色单独传达（形状 + 文字）
- [x] 序列色与状态色互不冒充
- [x] 没有给每个数据点都标数字
- [x] 网格线是发丝实线（虚线只用在目标阈值上——那是虚线的正当用法）
- [x] 小倍数共用同一纵轴
- [x] 每张图都有表格视图，数值不被 tooltip 独占
- [x] 图容器高度包含 x 轴文字，没有内部滚动条
- [x] 已实际渲染并肉眼检查过标签碰撞（第一版末点标签与目标线标签重叠，已修）
